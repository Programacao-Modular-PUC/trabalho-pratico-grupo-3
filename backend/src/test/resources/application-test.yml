spring:
  datasource:
    url: jdbc:h2:mem:noairnobnb;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

noairnobnb:
  security:
    jwt:
      issuer: noairnobnb-test
      secret: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
      expires-in-minutes: 120
